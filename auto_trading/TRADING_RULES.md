# 자동매매 규칙

작성 2026-08-11 · 최종 수정 2026-08-19

> 이 문서는 **코드에서 확인한 실제 동작**을 정리한 것이다. 값을 바꿀 때는 코드 상수와 이 문서를
> 함께 고쳐야 한다. 각 상수의 채택 근거(백테스트 수치)는 해당 파일 docstring에 있다.

## ⚠️ 2026-08-19 v8 전환 — 지금 무엇이 돌고 있나

| 전략 | 매수 | 청산 | 상태 |
|---|---|---|---|
| **v8** (신규) | `kiwoom_v8_strategy.py` 지정가 대기 | `kiwoom_v8_exit.py` | **가동 중** |
| fire (기존) | `kiwoom_fire_strategy.py` 15:18 시장가 | `kiwoom_trailing_stop.py` | **매수 중단**, 청산만 유지 |

- fire 매수는 `job/batch_runner.py` 의 `kiwoom_fire_buy` add_job 을 **주석 처리**해 껐다.
  방향이 v8과 정반대(당일 급등 추격 vs 급락 대기)이고 같은 예수금을 두고 경쟁한다.
- **청산은 소유권으로 분리**한다. v8이 주문을 낸 종목(`kiwoom_v8_strategy.v8_owned_codes()`)만
  v8 청산이 담당하고, 그 전부터 보유한 종목은 기존 트레일링이 그대로 담당한다.
  그래서 **아래 3·5절의 fire 청산 규칙은 기존 보유 종목에 대해 여전히 유효하다.**
- v8 상세는 [V8_SWITCHOVER.md](V8_SWITCHOVER.md), 근거는
  `C:\my-project\strategy-ab-backtest\ANALYSIS_V8.md`.

## ⚠️ 2026-08-20 — 실전/모의 두 계좌 동시 가동

| | 실전 (real) | 모의 (mock) |
|---|---|---|
| 프로세스 | `run.py` | **`run_mock.py`** (신규) |
| 매수 | v8 지정가 (15:55 스크리닝 / 60초 발주) | **fire 15:18 시장가** |
| 청산 | v8 exit + trailing(잔존분, 트레일링 OFF / 5영업일) | **trailing (트레일링 ON / 15영업일)** |
| 계좌 | 62959708 | 81328703 |
| 거래이력 | `trades_real.jsonl` | `trades_mock.jsonl` |
| 로그 | `trading.log` | **`trading_mock.log`** |
| 상태파일 | `*_real.json` | `*_mock.json` |

**별도 프로세스인 이유**: `kiwoom_fire_strategy` / `kiwoom_trailing_stop` 이 import 시점에 계좌번호와
상태파일 경로를 모듈 상수로 굳힌다. 한 프로세스에서 두 환경을 돌리려면 그 상수를 환경별
인스턴스로 쪼개야 하는데 그건 실계좌 코드를 통째로 건드리는 일이다. 프로세스를 나누면
`KIWOOM_ENV=mock` 하나로 계좌·토큰·호스트·상태·이력·로그가 전부 자동 분리된다.
레이트리밋(`_RATE_LIMIT_SLEEP`)도 프로세스 전역이고 키움 한도는 계좌·토큰당이라 실전 예산을
잠식하지 않는다. 모의 스케줄러에는 **pkl 갱신·스크래핑·Flask·node·v8 잡을 넣지 않았다** —
두 프로세스가 같은 pkl 을 쓰면 잠금 사고가 난다(fire 는 pkl 을 읽기만 한다).

`TRAILING_ENABLED` / `MAX_HOLD_DAYS` 는 `KIWOOM_ENV` 로 분기한다
(`kiwoom_trailing_stop.py` 해당 상수 정의부 주석).

> ⚠️ **모의 쪽 규칙(트레일링)은 3년 검증에서 4개 연도 전부 실전 규칙보다 나빴다**
> (비용후 −0.994% vs +0.067%, 아래 3절 표). 성능이 좋아서 켠 것이 아니라 **예전 방식을
> 실계좌와 나란히 관측하기 위해** 켠 것이다. 모의 성적을 실전 판단 근거로 쓸 때 감안할 것.

> 모의 계좌는 2026-08-20 신규 개설(3,000만원)이라 이전 이력·상태를 전부
> `*.bak_20260820_newaccount` 로 보관하고 비웠다. 남겨두면 예전 계좌의 쿨다운·포지션 상태가
> 새 계좌 매매를 조종한다(2026-08-14 실전 전환 당일의 반대 방향 사고).

**대시보드**: 관심종목 화면 '내 계좌' 탭 상단 `select`(실전/모의)로 두 계좌를 골라 본다.
조회와 **수동 매수/매도가 같은 값을 따라간다** — 화면은 모의인데 주문이 실전으로 나가는 것을
막기 위해서다. 자동매매 환경은 화면에서 바꿀 수 없다(`.env` 의 `KIWOOM_ENV`).

---

## 0. v8 전략 (2026-08-19~)

근거: 2023-07-17~2026-08-18 (3.09년), 거래 n=3,817, 기대 +2.80% (t=17.25),
CAGR +115.2% / MDD -28.1% / Sharpe 2.24, 3 fold 전부 양수(+122/+143/+84%).

### 매수

| 단계 | 시각 | 내용 |
|---|---|---|
| 스크리닝 | 평일 15:55 | 최근 20일 내 5일 상승률 +10% 이력 / 거래대금 ≥10억 / 종가 ≥700원 / 보통주만<br>지정가 = **당일 종가 × 0.70**, 유효 **10 거래일** |
| 아침 계산 | 장 첫 사이클 | 지정가 < 오늘 하한가(전일종가×0.70)인 후보 제외(주문 거부됨)<br>랭킹 재료(drop5/ma20/atr) 최신 확정 종가로 재계산. **API 0회** |
| 발주 | 60초 주기 | 실제 지정가 주문을 호가창에 걸어둔다 (대기 중 최고 지정가) |

⚠️ **폴링(감지 후 주문)은 쓰지 않는다.** 측정 결과 CAGR −41% / Sharpe −3.0.
전략의 엣지가 '순간 급락의 꼬리'라서 폴링의 포착은 역선택된다. `LIVE_REGAP=False`.

동시 주문 수 K = `min(MAX_OPEN_ORDERS 12, 남은슬롯, 예수금 ÷ 1회투입금)`.
1회 투입 **8%** 기준 189만원 → 10건, 500만원 → 12건. `SLOTS=25`는 자금상 도달 불가.

**CAGR 은 거의 전부 K 로 결정된다** (2026-08-19 측정, 500만원):
K=10 +28.9% / K=25 +46.1% / K=100 +81.2% / 무제한 +114.0%.
1회 투입을 8%로 낮춘 이유가 이것이다 — 같은 예수금으로 주문을 더 걸기 위해서다.
자세한 표는 V8_SWITCHOVER.md 8절.

주문 자리 배분 순위는 `live_gap`(체결 가능성)이 1순위, `score`가 동순위 판정이다.
점수가 높아도 지정가에 닿지 않으면 매수가 일어나지 않기 때문이다.
`WATCH_PRIORITY='score'`로 바꾸면 백테스트 랭킹 우선으로 되돌아간다.

### 매도 (`kiwoom_v8_exit.py`, 30초 주기 · 평가 순서대로)

| 순서 | 조건 | 수량 |
|---|---|---|
| 0 | 직전 관측가 대비 −35% 이상 급락 | **매도 보류** (액면분할/권리락 방어) |
| 1 | 현재가 ≤ 진입후최고가 − 3.0×ATR(14) | 잔량 **전량** |
| 2 | 현재가 ≤ 최고가 × 0.95 | 최초수량 × **1/2**, 발동 후 해제 / 고점 갱신 시 재무장 |
| 3 | 현재가 ≥ 진입가 × 1.20 | 최초수량 × **1/2** (1회) |
| 4 | 보유 10 영업일 | 잔량 **전량** |

`peak`은 장중 현재가로도 갱신하고, 15:52 `run_v8_eod()`가 당일 고가로 확정 + 재무장을 판정한다.

### v8 파라미터

| 상수 | 값 | 파일 |
|---|---|---|
| `RUN_MIN` / `RUN_LOOKBACK` | +10% / 20일 | v8_strategy |
| `DEPTH` / `VALID_DAYS` | 0.30 / 10 거래일 | v8_strategy |
| `AMOUNT_MIN` / `PRICE_MIN` | 10억 / 700원 | v8_strategy |
| `SLOTS` / `ALLOC` / `MIN_ORDER` | 25 / **8%** / 10만원 | v8_strategy |
| `MAX_OPEN_ORDERS` | 12 | v8_strategy |
| `WATCH_PRIORITY` / `GAP_TIE_BUCKET` | 'gap' / 2%p | v8_strategy |
| `LIVE_REGAP` | **False** (측정으로 폐기) | v8_strategy |
| `RESIZE_TOL` | 20% | v8_strategy |
| `CLAMP_TO_BAND` | False (하한가 미달 시 스킵) | v8_strategy |
| `ATR_MULT` / `TRAIL_PCT` / `TRAIL_FRAC` | 3.0 / 5% / 0.5 | v8_exit |
| `TP_PCT` / `TP_FRAC` / `MAX_HOLD_DAYS` | +20% / 0.5 / 10 | v8_exit |
| `ANOMALY_DROP` | −35% | v8_exit |

### v8 알려진 한계

1. **백테스트에 없던 예수금 제약.** 미체결 지정가가 예수금을 묶어 동시 8~10건이 한계다.
   백테스트는 도달 가능한 후보 전부를 대기시켰다. **체결 건수와 성과가 그만큼 낮아진다.**
2. **체결 기회의 54.3%는 '닿고 반등'이다** (최근 250거래일 6,546건 실측, 반등폭 중앙값 +2.9%).
   폴링으로 감지 후 주문하는 방식은 이 절반을 구조적으로 놓친다 — 호가창에 미리 걸어두는 것이 본질이다.
3. **시그널마다 독립 지정가**를 만들어 여러 개를 동시에 대기시키고, 주문은 그 중 최고가에
   건다. 체결되면 그 가격 이상만 소진하고 더 낮은 것은 계속 대기한다(원본과 동일).
4. **주문 자리 배분은 gap(오늘 필요한 하락폭 작은 순)이 1순위**다. 건당 기대수익만 보면
   반대가 좋지만(+2.34% vs −0.62%) CAGR 은 체결 횟수에도 좌우되어 gap 큰 순이 압도적이다.
   점수(`px_ma20` rho −0.105)는 동순위 판정에 쓰고, 기준 시점이 아침(전일 종가)이라
   백테스트(체결 시점)와 하루 차이가 있다.
5. **폭락 종목을 사므로 상장폐지 리스크 구간**이다. 백테스트에서 폐지/정지 거래 7.7%,
   기대수익 +0.72%(생존 종목 +2.98%).
6. **3.09년 한 사이클 결과**이고 **모의투자를 거치지 않았다.** 실계좌 체결 이력도 아직 없다.

---

## 1. 구성 (fire — 청산만 유효)

| 파일 | 역할 | 실행 주기 |
|---|---|---|
| `kiwoom_fire_strategy.py` | 신규 매수 (fire 신호) | 평일 **15:18 1회** |
| `kiwoom_trailing_stop.py` | 보유 종목 청산 | **30초** |
| `kiwoom_api.py` | 키움 REST 래퍼 (주문/조회/토큰) | — |
| `renew_kiwoom_token.py` | 접근토큰 갱신 | 매일 07:00 |
| `request_kiwoom_thema.py` | 테마 조회 (스케줄 미등록) | — |

스케줄 등록은 `job/batch_runner.py`. 환경 전환은 `.env`의 `KIWOOM_ENV=mock|real`
(`mockapi.kiwoom.com` / `api.kiwoom.com`, 계좌번호도 서로 다름).

### 상태 파일

| 파일 | 내용 | 잃으면 |
|---|---|---|
| `auto_trading/kiwoom_fire_state.json` | 종목별 마지막 매수일, 당일 매수 건수 | 쿨다운 초기화 → 2일 내 재매수 발생 |
| `auto_trading/kiwoom_trailing_state.json` | 종목별 고점·분할 진행상태 | 트레일링 진행 초기화 → 분할 재시작 |

두 파일 경로는 `os.path.dirname(__file__)` 기준이다. **코드를 다른 디렉터리로 옮기면 상태 파일도
같이 옮겨야 한다.** 로그/이력(`logs/kiwoom_trading/`)은 루트 기준이라 영향 없다.

---

## 2. 매수 조건 (fire — **중단됨**, 기록용)

**실행: 평일 15:18 단 1회.** 15:20을 넘기면 `is_market_open()`이 False라 주문 없이 스킵된다.

> 왜 15:18인가 — 백테스트가 '신호일 종가 매수'를 가정한다. pkl 정규장 마지막 갱신이 15:10,
> KRX 정규장 종료가 15:20이므로 그 사이인 15:18에 평가해 종가에 붙이면서도 정규장 안에서
> 체결시킨다. 실측으로 체결가→종가 드리프트는 금액가중 **-0.08%** 수준이다(2026-08-11, 11종목).

### 통과해야 하는 게이트 (순서대로)

| # | 조건 | 값 / 상태 |
|---|---|---|
| 1 | 계좌 정보 | `KIWOOM_ACNT_NO` / `KIWOOM_ACNT_PWD` |
| 2 | 장 시간 | KRX 정규장 09:00~15:20 |
| 3 | 시장폭 레짐 | `BREADTH_MIN = 0.0` → **OFF** (breadth는 로그용으로만 계산) |
| 4 | fire SQL 조건 | 아래 |
| 5 | **reserved 교집합** | 화면에서 '자동매수 대상' 체크한 종목만. 체크 0개면 그날 매수 0 |
| 6 | 일일 한도 | `BUY_SLOTS = 20` **성공 매수** 도달 시 중단 (skip은 슬롯 미소진) |
| 7 | 쿨다운 | `COOLDOWN_DAYS = 2`. 보유 중이어도 쿨다운만 지나면 추가매수 |
| 8 | 현재가 조회 | `price > 0` |
| 9 | 예산 | 배정 예산으로 1주 이상 살 수 있어야 함 |

### fire SQL 조건

`target = 'interest'`, 집계 기간 = **오늘 − 6일 ~ 오늘** (`FIRE_WINDOW_DAYS = 6`)

- 시가총액 > **700억**
- 신호 발생일수(`signal_days`) **2 ~ 3일**
- 평균 거래대금 > **40억**, 최근 거래대금 > **40억**
- 최근/평균 거래대금 > **0.5**
- 09시 이후 실행이면 **오늘 신호가 있어야 함** (`last_date = 오늘`)

> ⚠️ `total_rate <= 20`, `today_price_change_pct 3~8`, `increase_per_day 3~6` 조건은
> SQL에 **주석 처리되어 비활성**이다. 총상승률 상한이 없어 통과 종목이 많다
> (2026-08-11 실측 **89종목** 통과, reserved 19종목과 교집합 18종목).
> 2026-08-05에 H2 필터(20일 신고가 -1.6% 이내 + 당일 등락률 +12% 이상)를 제거한 결과다.

### 매수 순서

**총상승률 낮은 순** (파싱 실패는 맨 뒤). 이 정렬이 곧 대상 선정이다 — 위에서부터 훑다가
`BUY_SLOTS`를 채우면 멈춘다.

> 근거: 2026-06~08 3,255건을 실제 청산규칙으로 시뮬레이션(수수료 차감 후) —
> 높은 순 +0.77% / 무작위 30회 평균 +0.85% / **낮은 순 +2.35%**.
> 많이 오른 종목을 쫓는 것보다 덜 오른 종목을 담는 쪽이 유리했다.

### 사이징

```
가용현금   = 총자산 − 보유종목 평가금액
매수한도   = 가용현금 × 0.65                          # CASH_DEPLOY_RATIO
종목당예산 = min( 남은한도 ÷ min(남은후보수, 남은슬롯수),  # 순차 재분배
                 매수한도 ÷ 5,                        # POS_CAP_DIVISOR (= BUY_SLOTS)
                 남은한도 )
수량       = 종목당예산 // 현재가                      # 정수. 0이면 skip하고 다음 후보로
주문       = 시장가, KRX
```

- **`CASH_DEPLOY_RATIO`는 '보유 목표 비율'이 아니다.** '그날 남은 현금 중 신규 매수에 쓸 최대
  비율'이다. 정상상태 보유비율은 `u = h·r/(1+h·r)` (h=평균 보유일). 현재 청산규칙에서 h=3.48일,
  r=0.65의 **실측 보유비율은 약 79%(현금 약 21%)**다.
- 주문 거부 시(`order_accepted()` 실패): 이력 미기록, **슬롯·한도 미소진**, 다음 후보로 진행.

> ### 2026-08-18 변경: 예산 분모 수정 + `CASH_DEPLOY_RATIO` 0.70 → 0.65
>
> **버그.** 재분배 분모가 '남은 후보 수'였다. 후보가 슬롯보다 많은 날엔 한도를 후보 수로 잘게
> 쪼개 놓고 슬롯이 먼저 소진돼 한도의 `(후보−슬롯)/후보`가 통째로 남았다. 반대로 후보가 적고
> `POS_CAP_DIVISOR`가 크면 종목당 상한에 걸려 또 남았다. 결과적으로 **한도 소진율이 후보 수에
> 따라 28~70%로 널뛰었다.**
>
> 실제 사고 — 2026-08-14: 가용현금 1,989,709원, 후보 7종목, 당시 divisor 20 → 종목당 상한
> 69,640원. 현대오토에버(489,000원)는 1주도 못 사 skip, 나머지 6종목 정수 내림으로
> **325,840원(자산의 16.4%)만 집행**되고 현금 83%가 남았다.
>
> **수정 후** 분모가 `min(남은후보, 남은슬롯)`이라 한도 소진율이 후보 수와 무관하게 ~88%로
> 안정된다(후보 7/10/무제한 모두 실측 보유비율 79.1/79.3/79.3%). 사이징이 예측 가능해진 것이
> 이 수정의 핵심이다.
>
> **ratio.** 수정으로 노출이 올라가므로(0.70에서 보유 82%) ratio를 0.65로 내려 요청받은
> 현금 20%에 맞췄다. 부트스트랩 25회(하루 후보 80% 표집, 초기자본 199만원, 슬롯5/divisor5,
> 왕복비용 0.2%, 후보 상위 7개 제한):
>
> | ratio | 현금% | 총수익 평균 | 표준편차 | 최저 | 음수비율 | MDD 평균 |
> |---|---|---|---|---|---|---|
> | 0.50 | 30.8 | +21.2% | 15.4 | −15.9% | 8% | −39.0% |
> | 0.55 | 27.2 | +19.8% | 15.2 | −18.8% | 8% | −40.7% |
> | 0.60 | 23.9 | +18.5% | 15.6 | −20.8% | 8% | −42.5% |
> | **0.65** | **20.9** | **+17.1%** | 16.2 | **−24.0%** | **12%** | **−43.9%** |
> | 0.70 | 18.1 | +15.6% | 16.0 | −26.1% | 12% | −45.2% |
> | 0.80 | 13.2 | +10.6% | 17.1 | −31.6% | 28% | −47.8% |
> | 1.00 | 5.3 | +1.3% | 17.3 | −37.7% | 52% | −51.7% |
>
> ⚠️ **노출과 성과가 단조 역상관이다** — 현금을 줄일수록 평균이 내려가고 꼬리가 나빠진다.
> §6의 '노출을 늘리는 쪽이 항상 불리하다'와 방향이 같다. **현금 20%는 성과 최적점이 아니라
> 요청받은 목표치**이고, 0.50 대비 평균 −4.1%p / 최저 −8.1%p가 그 대가다.
> 0.65를 고른 건 목표(20%)에 가장 가까우면서 0.70을 전 지표에서 상회하기 때문이다.
>
> ⚠️ 위 수치는 reserved 교집합을 '종가위치 상위 7개'로 근사한 것이라 **낙관 방향**이다
> (실제로는 내가 체크한 종목이 상위 7개라는 보장이 없다). 생존편향도 포함한다.
>
> ### ⚠️⚠️ 2026-08-19: 위 표의 절대 수치는 상한가 인공물에 오염돼 있다
>
> 종가위치 상위 N개를 고르는 방식이 **상한가 종목을 6.5배 농축한다**(전체 후보의 2.8% →
> 상위5의 18.1%). 종가 = 고가는 상한가의 전형적 형태이고, 상위5의 64.8%가 종가위치 ≥ 0.99다.
> 그 상한가권 건당이 +3.760%로 표 전체를 끌어올린다. **실제로는 상한가에 매도 잔량이 없어
> 15:18 시장가 매수가 체결되지 않는다.**
>
> | 집단 | 건수 | 비용후 | 상한가권 비중 |
> |---|---|---|---|
> | 전체 후보 | 31,421 | +0.197% | 2.8% |
> | 전체 − 상한가권 | 30,535 | **+0.070%** | 0% |
> | 일별 종가위치 상위5 | 4,083 | +0.485% | 18.1% |
> | 일별 상위5 − 상한가권 | 4,067 | **−0.124%** | 0% |
>
> 즉 상한가를 빼면 건당이 마이너스가 된다. **위 ratio 표의 +17.1% 같은 값을 실계좌 기대치로
> 읽지 말 것.** 다만 ratio 간 *상대* 비교(노출을 줄일수록 낫다)는 방향이 유지되고, 기대값이
> 음수라면 노출을 줄이는 근거가 오히려 강해진다 — 그래서 0.65 선택 자체는 되돌리지 않았다.
>
> 상한가 제외 시 연도별: 2023 −0.018 / 2024 −0.052 / 2025 +0.303 / 2026 +0.008 —
> **2025년 하나만 플러스다.** 이 +0.070%는 아래 §3의 '비용 후 +0.067% = 간신히 본전 위'와
> 거의 일치한다(종전 평가가 맞았다).
>
> 상세·오염 범위: `auto_trading/backtest/cagr_estimate.py` docstring
>
> 검증: `auto_trading/backtest/cash_ratio_test.py`

---

## 3. 매도 조건 (fire/trailing — v8 미소유 종목에 유효)

**실행: 30초마다.** `is_market_open()` 통과 구간 = 평일 **09:00~15:20(KRX 정규장)만**.
08:50~09:00(동시호가)과 15:20~15:30(KRX 종가 단일가)은 연속체결이 아니라 제외.
NXT 프리(08:00~08:50)·애프터(15:30~20:00)는 **2026-08-18에 제외했다** — 아래 참고.

> ⚠️ **NXT 시간대에는 주문이 나가지 않는다 — 모의/실계좌 둘 다.**
>
> | 환경 | NXT 응답 | 원인 |
> |---|---|---|
> | mock | `RC9000: 모의투자에서는 해당업무가 제공되지 않습니다` | 모의투자가 NXT 미지원 |
> | **real** | **`407022: 주문이 불가능한 주문종류입니다`** | **NXT가 시장가를 안 받는다** |
>
> `buy_market`/`sell_market`은 항상 `trde_tp='3'`(시장가)를 보내는데(`kiwoom_api.py:322-327`)
> NXT 프리/애프터마켓은 지정가만 받는다. 2026-08-18 실계좌 실측 — 08:31, 08:57 주문 2건이
> 위 코드로 거부됐다.
>
> **따라서 실질 청산 가능 구간은 09:00~15:20(KRX 정규장)뿐이다.** 실계좌로 바꿔도 달라지지
> 않았다(종전 이 문서는 "실계좌로 전환하면 그 구간이 열린다"고 적었는데 사실이 아니었다).
> 프리마켓 갭 하락은 09:00까지 방치된다.
>
> **API 자체는 NXT를 지원한다.** `place_order`의 `dmst_stex_tp`가 `'KRX' | 'NXT' | 'SOR'`를
> 받고 `trde_tp`가 `'3'` 시장가 / `'0'` 보통(지정가)를 받는다(`kiwoom_api.py:299-319`).
> 막힌 건 거래소가 아니라 **주문 종류**다 — 래퍼가 시장가만 보내는데 NXT 프리/애프터마켓은
> 지정가만 받는 것으로 보인다(407022가 "주문종류"를 지목한다). 확정하려면 실계좌에서
> `trde_tp='0'` + 호가로 1주 테스트가 필요하다 — 아직 안 해봤다.
>
> 거부 자체는 `order_accepted()`가 걸러 이력·상태를 건드리지 않고 재시도하므로 위험하지 않았다.
> 다만 08:00~08:50에 청산 조건이 걸리면 50분간 30초마다 거부 로그만 쌓이고 결국 09:00에
> 체결되므로, **결과가 같으면서 로그만 더러워져** `is_market_open()`에서 구간을 제거했다.
> 다시 열려면 NXT 구간만 지정가로 보내는 분기를 먼저 넣어야 한다.

### 평가 순서 (먼저 걸리는 것이 우선)

#### 0. 이상 감지 → **매도하지 않고** 자동매매 정지

- 현재가 ≤ 0 (시세 조회 실패)
- 직전 관측가 대비 **−35% 이상** 급락 (`ANOMALY_DROP`, 일일 제한폭 ±30% 초과)
- 손실률 ≤ **−25%** (`ANOMALY_RATE`, 30초 잡 구조상 정상 도달 불가)

액면분할·권리락 당일 증권사가 현재가만 먼저 조정하고 평단가/수량은 늦게 조정하는 구간을
막는 장치다. 값이 정상 범위로 돌아오면 자동 재개.

> ### ⚠️ 2026-08-14 변경: 트레일링 비활성화 + 보유 5영업일 상한
>
> 현재 운영 규칙은 **손절 −6% + 보유 5영업일 상한** 두 가지뿐이다.
> `TRAILING_ENABLED = False`이므로 아래 2·3·4번(트레일링/목표가/정체보호)은 **동작하지 않는다.**
>
> 3년치(2023-04~2026-08, 신호 30,205건) 연도별 검증에서 트레일링이 4개 연도 **전부** 손실을
> 냈다(현행 상회 0/4). 같은 신호에 청산만 바꾼 연도별 평균 %(왕복비용 0.2% 반영 전):
>
> | 청산 방식 | 2023 | 2024 | 2025 | 2026 | 전체 | 비용후 |
> |---|---|---|---|---|---|---|
> | 트레일링(최대15일) | −1.009 | −1.290 | −0.455 | −0.384 | −0.794 | −0.994 |
> | 트레일링 튜닝(활성3%/gap3%) | −0.627 | −0.724 | −0.378 | −0.362 | −0.526 | −0.726 |
> | **손절−6% + 5일 보유** | −0.120 | −0.075 | **+0.578** | **+0.705** | **+0.267** | **+0.067** |
>
> 이 신호의 수익은 1~3일에 몰려 있는데(1일 +0.106% / 3일 +0.085% / 10일 −0.918% / 20일 −1.366%)
> 트레일링은 +7%까지 올라야 켜지고 5%p 되돌릴 때까지 안 팔아 수익 구간이 끝난 뒤에 나온다.
>
> ⚠️ 비용 후 +0.067%는 '간신히 본전 위'다. 2023·2024년은 개선 후에도 마이너스이고,
> pkl에 상장폐지 종목이 빠져 있어(생존편향) 실제는 이보다 낮다.
>
> 검증: `auto_trading/backtest/exit_validation_3y.py`
> 되돌리려면 `TRAILING_ENABLED = True`.

#### 1. 손절 → 잔여 **전량**

- `rate <= -6%` (`STOP_LOSS_RATE`)
- 트레일링 활성 이력이 있으면 `ARMED_GIVEBACK_STOP = -6%` 적용
  → **현재 두 값이 같아 동작 차이 없음.** 되돌림만 좁히려면 `ARMED_GIVEBACK_STOP`만 조정.

#### 1-2. 보유기간 상한 → 잔여 **전량** (2026-08-14 신규)

- 진입일로부터 **5영업일**(`MAX_HOLD_DAYS`) 경과 시 시장가 전량 청산
- 손절 다음, 트레일링보다 먼저 평가한다
- 기준일은 `pos_state['entry_date']` — 실제 매수일이 아니라 **상태를 처음 만든 날**이다.
  추가매수로 상태가 리셋되면 보유기간도 그때부터 다시 센다(평단가가 바뀌므로 손절선과 동일한 취급).
- 영업일 계산에 **공휴일은 반영하지 않는다** → 실제 거래일보다 크거나 같아 상한에 약간 일찍
  걸린다. **고치지 말 것** — 일찍 나가는 쪽이 실측으로 더 낫다(아래 표 D).
- 청산 이력의 `reason`은 `max_hold`

**언제 체결되나 (2026-08-18 확인).** 조건은 `held >= 5`라 그날 **첫 사이클에 바로** 걸린다.
NXT 프리마켓은 위 407022로 막혀 있고 2026-08-18부터 `is_market_open()`에서 아예 제외했으므로,
**09:00 KRX 개장 직후 첫 사이클에 시장가로 체결**된다. 즉 실질 **개장가** 청산이다. 종가가 아니다.

### 시가 청산 vs 종가 청산 (2026-08-18 실측)

`auto_trading/backtest/exit_open_vs_close.py` — 3년 31,401건, 왕복비용 0.21% 차감

| 청산 방식 | 비용후 | 중앙 | 승률 | 2023 | 2024 | 2025 | 2026 |
|---|---|---|---|---|---|---|---|
| A. 종가[5일차] — 백테스트 가정 | −0.021 | −2.634 | 38.0% | −0.189 | −0.169 | +0.302 | −0.080 |
| **B. 시가[5일차] — 라이브 실제** | **+0.113** | −1.905 | 39.5% | +0.001 | −0.016 | +0.339 | +0.102 |
| C. 종가[4일차] | −0.071 | −1.952 | 38.9% | −0.235 | −0.138 | +0.189 | −0.161 |
| D. 시가[4일차] — 공휴일 1일 낀 경우 | **+0.142** | −1.282 | 41.5% | +0.027 | +0.001 | +0.396 | +0.108 |

**라이브(B)가 백테스트 가정(A)보다 +0.134%p 낫다. 4개 연도 전부.** 지금까지의 모든 백테스트가
라이브 성과를 **과소평가**하고 있었다 — 괴리가 유리한 방향이다.

분해: `종가[4일차] −0.071` → 오버나이트 갭 **+0.184** → `시가[5일차] +0.113` → 5일차 세션
**−0.134** → `종가[5일차] −0.021`

4일차까지 손절 안 된 19,622건(62.5%) 기준, 갭은 평균 +0.262%/중앙 0.000%(우측 꼬리)이고
5일차 낮 세션은 평균 −0.207%/중앙 −0.391%로 일관되게 마이너스다.
**아침에 팔면 갭은 먹고 낮 하락은 피한다** — 이것이 B > A의 정체다.

> 위 D > B를 보고 `MAX_HOLD_DAYS` 5→4를 검증했으나 **기각됐다**
> (`auto_trading/backtest/max_hold_sweep.py`, 2026-08-18). H를 2~7로 훑으면 건당이 전부
> **+0.088 ~ +0.137** 구간이고 단조성이 없다(H=7이 H=2와 사실상 같고 H=3이 H=4보다 낮다).
> H=4가 H=5를 4개 연도 전부 앞서지만 폭이 +0.031%p로 곡선의 비단조 폭보다 작다.
> 포트폴리오 수치는 전체집합과 부분집합의 순서가 뒤집혀(경로 노이즈) 판단 근거로 쓸 수 없었다.
> **`MAX_HOLD_DAYS`는 5 유지.**

#### 2. 트레일링 고점 갱신

- `rate >= +7%` (`TRAIL_ACTIVATE_RATE`)부터 `peak_rate` 추적 시작

#### 3. 트레일링 매도 — 세 조건 **모두** 충족 시 발동

- `peak_rate >= +7%` (활성)
- **직전 매도 때보다 높은 새 고점** (같은 고점에서 반복 매도 방지)
- `rate <= 트리거선`, **트리거선 = max(peak − 5%p, +3%)**
  (`TRAIL_GAP = 5%p`, `MIN_PROFIT_FLOOR = +3%`)

매도 수량:

| 상황 | 수량 | reason |
|---|---|---|
| 트리거선이 보호선(+3%)에 붙어 발동 | **잔여 전량** | `trailing_floor` |
| 그 외 | **1/3**씩 (최대 3분할, 3번째는 잔여 전량) | `trailing_gap` |

> 활성 +7% − 갭 5%p = +2% < 보호선 +3%이므로 **고점이 8%를 넘어야** 트리거선이 보호선 위로
> 올라가 정상적인 고점−5%p 트레일링이 된다. 고점 7~8% 구간은 항상 보호선에 걸려 전량 청산된다.
> 보호선 트리거를 전량으로 하는 근거: 1년 재현 1,700건 중 87건, 1/3만 남기는 것보다
> 전량 정리가 승률·기하평균·최근 분기 안정성 모두 나았다(기하평균 +0.199% → +0.220%).

#### 4. 목표가 → **비활성** (`TARGET_RATES = []`)

고정 사다리(10/15/20%)는 상승 종목을 일찍 끊어 성과를 깎는 것으로 검증돼 껐다.
익절은 전적으로 트레일링이 담당한다.

#### 5. 정체 보호 → 잔여 **전량**

- 트레일링이 한 번 나간 뒤(`last_sold_peak` 존재), 그 이후 **새 고점 없이**
- `rate <= (직전 트리거선 − 6%p)` (`STALL_GAP`)

트레일링 매도 후 새 고점이 안 나오면 재발동되지 않아 잔여 물량이 무보호로 노출되는 것을 막는다.
고점이 높았던 종목에 주로 작동한다(예: 고점 20% → 트리거 15% → 9%에서 정리).

### 상태 리셋

추가 매수로 평단가가 바뀌면 `peak_rate`·분할 진행상태를 **전부 버리고** 새 평단가 기준으로
처음부터 다시 평가한다. 수익률(`rate`)이 평단가 기준 값이라 예전 %는 같은 척도가 아니다.

---

## 4. 주문 결과 검증 (매수·매도 공통)

`order_accepted(result)` — `auto_trading/kiwoom_trailing_stop.py`

```python
return_code == 0  AND  ord_no 존재
```

| 응답 | 판정 |
|---|---|
| `{'ord_no': '0157630', 'return_code': 0, 'return_msg': '모의투자 매수주문완료'}` | 통과 |
| `{'return_msg': '[2000](RC9000:모의투자에서는 해당업무가 제공되지 않습니다.)', 'return_code': 20}` | 거부 |
| `{'return_msg': '[2000](RC4058:모의투자 장종료)', 'return_code': 20}` | 거부 |

거부 시:

- 거래 이력(`trades.jsonl`)에 **기록하지 않음**
- 포지션 상태(`pos_state`)를 **건드리지 않음** → 다음 30초 사이클에 재시도
- fire 매수는 **슬롯·한도도 미소진**

> 2026-08-11 도입. 이전에는 주문이 거부돼도 이력이 남고 `exited=True`로 바뀌어 **그 종목이
> 트레일링 관리에서 영구히 빠졌다.** 오염된 기록 5건은 제거했다(백업 `trades.jsonl.bak_20260811`).
>
> ⚠️ 이것은 '주문 접수' 확인이지 '체결 완료' 확인이 아니다. 실제 체결값은 아래 정산이 채운다.
>
> ⚠️ `sell_market`/`buy_market`을 스텁으로 대체하는 재현·검증 스크립트는 이제 성공 형태
> (`{'return_code': 0, 'ord_no': ...}`)를 반환해야 한다. 그렇지 않으면 매도가 전부 건너뛰어진다.

### 체결 정산 (2026-08-12 추가)

주문 시점에 기록되는 `price`/`qty`는 **조회가와 주문수량**이다. 평일 **20:10**에 `reconcile_fills()`가
당일 체결내역(`ka10076`)을 조회해 실제 값을 채운다 — `fill_price`, `fill_qty`, `unfilled`,
`cmsn`(수수료), `tax`(거래세), `slippage`, `fill_pnl`.

`ord_no`로 매칭하며, 없으면 (종목+구분+수량+시각 120초 내) 폴백한다. 수동 실행은
`python -m auto_trading.kiwoom_trailing_stop --reconcile [--dry-run] [--date YYYY-MM-DD]`.
`ka10076`은 날짜 파라미터가 없어 **당일분만** 주므로 소급 정산은 같은 날에만 가능하다.

**`pnl` 필드는 수수료·세금을 반영하지 않는다.** 실질 손익을 보려면 `cmsn`+`tax`를 따로 빼야 한다.

---

## 5. 파라미터 한눈에 (fire/trailing — v8 은 0절 참고)

| 상수 | 값 | 파일 |
|---|---|---|
| `FIRE_WINDOW_DAYS` | 6일 | fire |
| `BREADTH_MIN` | 0.0 (게이트 OFF) | fire |
| `CASH_DEPLOY_RATIO` | 0.65 (현금 약 21% 목표) | fire |
| `BUY_SLOTS` | 5 | fire |
| `POS_CAP_DIVISOR` | 5 (= `BUY_SLOTS`) | fire |
| `CLOSE_POS_MIN` | 0.6 (종가위치 필터) | fire |
| `COOLDOWN_DAYS` | 2 | fire |
| `CHECK_DISPLAY_LIMIT` | 20 (`--check` 표시용, 매수 무관) | fire |
| `STOP_LOSS_RATE` | −6% | trailing |
| `ARMED_GIVEBACK_STOP` | −6% | trailing |
| `TARGET_RATES` | `[]` (비활성) | trailing |
| `TRAIL_ACTIVATE_RATE` | +7% | trailing |
| `TRAIL_GAP` | 5%p | trailing |
| `MIN_PROFIT_FLOOR` | +3% | trailing |
| `STALL_GAP` | 6%p | trailing |
| `ANOMALY_DROP` / `ANOMALY_RATE` | −35% / −25% | trailing |

---

## 6. 실제 발동 비율

현재 청산규칙으로 재생성한 백테스트 12,034건 (2025-09-09 ~ 2026-08-10,
`auto_trading/backtest/fire_backtest_regen.py`):

| 첫 청산 사유 | 비중 | 평균 수익률 |
|---|---|---|
| `stop_loss` | **53%** | −6.34% |
| `trailing_gap` | 41% | +5.64% |
| `trailing_floor` | 8% | +1.33% |

청산 시퀀스 상위:

```
stop_loss                              5,971   절반이 그냥 손절
trailing_gap|stall                     1,629   1/3 익절 후 정체보호로 나머지 정리
trailing_gap|trailing_gap|trailing_gap   944   3분할 완주 (이상적)
trailing_gap|trailing_gap|stall          924
trailing_gap|giveback_stop               773   1/3 익절 후 −6%까지 되돌림
```

**건당 기대값이 0 근처다** — 일중 경로 근사의 비관/낙관 극단이 **−0.740% ~ +0.161%**이고,
실제 체결 이력(2026-07-13~08-11 자동청산 74건)이 **−0.059%**로 그 범위 안에 들어온다.
왕복비용 0.2%를 얹으면 확실히 마이너스다.

> 기대값이 0 이하인 동안은 **노출을 늘리는 쪽이 항상 불리하다.** `POS_CAP_DIVISOR`를 낮추거나
> `CASH_DEPLOY_RATIO`를 올리는 변경은 기대값이 확실히 플러스임을 먼저 입증한 뒤에만 의미가 있다.
> (한때 `POS_CAP_DIVISOR=10`으로 내렸다가 되돌렸다 — 근거였던 구 CSV가 목표가 +15%가 살아
> 있던 시절 규칙이라 기대값을 +0.39%로 과대평가하고 있었다.)

---

## 7. 검증 도구

| 스크립트 | 용도 |
|---|---|
| `auto_trading/backtest/fire_backtest_regen.py` | 현재 규칙으로 백테스트 CSV 재생성. 청산 상수를 코드에서 직접 import하므로 상수를 바꾸면 같이 바뀐다. `--no-same-day-trigger`로 낙관 극단 |
| `auto_trading/backtest/fire_sizing_backtest.py` | 사이징 규칙 포트폴리오 재시뮬레이션 (`compare cap sweep periods monthly mechanism today ratio`) |
| `python -m auto_trading.kiwoom_fire_strategy --check` | 매수 없이 오늘 후보/레짐만 출력 |
| `python -m auto_trading.kiwoom_trailing_stop --buy <코드> [수량]` | 수동 매수 |
| `python -m auto_trading.kiwoom_trailing_stop --sell <코드> <수량>` | 수동 매도 |

재생성 CSV는 `logs/kiwoom_trading/fire_backtest_result_current{,_optimistic}.csv`.
`logs/`가 gitignore 대상이라 `git add -f`로 추적한다.

---

## 8. 알려진 한계

1. **reserved 이력이 없어 백테스트에 교집합을 재현할 수 없다.** 재생성 CSV는 '전 종목을 체크
   했다면'에 해당하는 상한선이다. 실제로는 체크한 종목만 사므로 **종목 선택이 사이징
   파라미터보다 성과에 훨씬 큰 영향을 준다**(같은 조건에서 27.0% vs 37.2%로 갈렸다).
2. **일봉 기반이라 30초 잡의 일중 경로를 재현할 수 없다.** 하루 안에서 고점을 먼저 찍었는지
   저점을 먼저 찍었는지 알 수 없어, 비관/낙관 두 극단을 같이 내고 그 범위로 해석한다.
3. **NXT 시간대 동작을 모의투자로 검증할 수 없다.** 실계좌에서 처음 겪게 된다.
4. **모의투자 거래비용이 실계좌보다 훨씬 높다.** 2026-08-11 실측 편도 매수 0.345% / 매도 0.546%
   = **왕복 0.881%**. 실계좌는 수수료가 0.015% 수준이라 거래세 0.18%를 더해도 왕복 약 0.21%로
   추정된다. **mock 실적으로 전략을 평가할 때 반드시 감안할 것** — 위 백테스트의 왕복 0.2%
   민감도는 실계좌 기준으로는 타당하다.
5. `interest_v2` 신호는 2026-08-11 도입으로 데이터가 1일뿐이라 백테스트 불가.
6. `trades.jsonl`과 `asset_baseline.json`을 비교할 때 **기간을 맞춰야 한다.** 후자는 당월
   스냅샷이고 전자는 전 기간이다. 월별로 쪼개면 맞는다(2026-08 실현손익 −67,512원 vs
   자산변화 −76,547원, 차이 9,035원).

### 실측으로 확인된 것 (2026-08-11 21건 소급 정산)

- **슬리피지는 무시할 수준이다** — 매수 +0.034%, 매도 −0.024%. 21건 중 12건은 체결가가 주문 시점
  조회가와 정확히 일치했다. **백테스트가 '조회가에 체결된다'고 가정한 것은 타당하다.**
- **미체결 0건** — 전량 시장가가 정규장에서 모두 전량 체결됐다. 다만 보장은 아니므로 정산이
  `unfilled`를 계속 감시한다(부분체결 시 ERROR 로그).
